

int divisibleSumPairs(int n, int k, vector<int> ar) {
    int a=ar.size();
    int c=0;
    for (int i=0 ; i<a-1 ; i++) {
        int sum=0;
        for (int j=i+1 ; j<a ; j++) {
            sum=ar[i]+ar[j];
            if (sum%k==0) c++;
        }
    }
    return c;
}
