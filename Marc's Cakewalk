

long marcsCakewalk(vector<int> calorie) {
    sort(calorie.rbegin(),calorie.rend());
    
    int n=calorie.size();
    long ans=0;
    for (int i=0 ; i<n ; i++) {
        ans = ans + long(calorie[i]) * pow(2,i);
    }
    return ans;
}
