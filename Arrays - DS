

vector<int> reverseArray(vector<int> a) {
    int n= a.size();
    int i=0,j=n-1;
    while(i<=j) {
        int temp=a[i];
        a[i]=a[j];
        a[j]=temp;
        i++;
        j--;
    }
    return a;
}
